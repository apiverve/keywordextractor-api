plugins {
    id 'java-library'
    id 'maven-publish'
}

group = 'com.apiverve'
version = '1.1.12'

java {
    sourceCompatibility = JavaVersion.VERSION_1_8
    targetCompatibility = JavaVersion.VERSION_1_8
}

dependencies {
    implementation 'org.json:json:20231013'
}

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.apiverve'
            artifactId = 'keywordextractor-api'
            version = '1.1.12'

            from components.java

            pom {
                name = 'Keyword Extractor'
                description = 'Keyword Extractor is a simple tool for extracting keywords from a web page. It returns the keywords and the frequency of each keyword.'
                url = 'https://apiverve.com'

                licenses {
                    license {
                        name = 'MIT License'
                        url = 'https://opensource.org/licenses/MIT'
                    }
                }

                developers {
                    developer {
                        id = 'apiverve'
                        name = 'APIVerve'
                        email = 'hello@apiverve.com'
                    }
                }

                scm {
                    connection = 'scm:git:git://github.com/apiverve/keywordextractor-api.git'
                    developerConnection = 'scm:git:ssh://github.com/apiverve/keywordextractor-api.git'
                    url = 'https://github.com/apiverve/keywordextractor-api'
                }
            }
        }
    }
}
